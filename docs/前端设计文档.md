# 无缝钢管与筛管管理系统 — 前端设计文档

> **文档版本**: v1.1
> **创建日期**: 2026-05-19
> **基于后端设计**: docs/详细设计文档.md
> **技术栈**: React 19 + Ant Design 5 + TypeScript + Vite

---

## 修订记录

| 版本 | 日期 | 修订内容 | 修订人 |
|------|------|---------|--------|
| v1.0 | 2026-05-19 | 初始版本 | - |
| v1.1 | 2026-05-19 | Axios 401 改为先刷新后登出；Zustand 纯化状态（login 移至 Hook）；移除全局 loading；移除 route loader；使用系统回退字体；增加 MSW mock 设计；staleTime 调整为 2min | - |

---

## 目录

1. [技术栈与依赖](#1-技术栈与依赖)
2. [项目目录结构](#2-项目目录结构)
3. [页面路由设计](#3-页面路由设计)
4. [页面列表与功能](#4-页面列表与功能)
5. [布局结构](#5-布局结构)
6. [组件树](#6-组件树)
7. [状态管理方案](#7-状态管理方案)
8. [API 层设计](#8-api-层设计)
9. [认证与鉴权流程](#9-认证与鉴权流程)
10. [国际化方案](#10-国际化方案)
11. [Ant Design 5 主题定制](#11-ant-design-5-主题定制)
12. [前端数据流总图](#12-前端数据流总图)
13. [后续实施建议](#13-后续实施建议)

---

## 1. 技术栈与依赖

### 1.1 技术选型

| 层次 | 选型 | 版本 | 说明 |
|------|------|------|------|
| **构建工具** | Vite | 6.x | 开发服务器极速启动，ESBuild 编译 |
| **UI 框架** | React | 19.x | — |
| **语言** | TypeScript | 5.x | 严格模式 |
| **组件库** | Ant Design | 5.x | 企业级组件库，表格/表单/菜单成熟 |
| **路由** | React Router | 7.x | 支持嵌套路由、loader、action |
| **服务端状态** | TanStack Query | 5.x | 缓存、后台刷新、乐观更新 |
| **客户端状态** | Zustand | 5.x | 轻量、无 boilerplate |
| **HTTP 客户端** | Axios | 1.x | 拦截器、请求/响应转换 |
| **国际化** | react-i18next | 15.x | 与 i18next 生态集成 |
| **表格** | Ant Design Table | — | 内置于 Ant Design 5 |
| **图表** | @ant-design/charts | 2.x | 基于 G2Plot，与 Ant Design 风格一致 |
| **表单** | Ant Design Form | — | 内置于 Ant Design 5 |
| **日期处理** | dayjs | 1.x | Ant Design 5 内置 dayjs |
| **代码规范** | ESLint + Prettier | — | — |

### 1.2 package.json 核心依赖

```json
{
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^7.0.0",
    "antd": "^5.20.0",
    "@ant-design/icons": "^5.5.0",
    "@ant-design/charts": "^2.1.0",
    "@tanstack/react-query": "^5.60.0",
    "zustand": "^5.0.0",
    "axios": "^1.7.0",
    "react-i18next": "^15.0.0",
    "i18next": "^24.0.0",
    "dayjs": "^1.11.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "eslint": "^9.0.0",
    "prettier": "^3.4.0",
    "msw": "^2.6.0",
    "@faker-js/faker": "^9.3.0"
  }
}
```

---

## 2. 项目目录结构

采用 **Feature-Sliced Design** 变体，按功能模块组织代码。

```
pipe-management-frontend/
├── index.html
├── vite.config.ts                  # Vite 配置（代理、插件）
├── tsconfig.json
├── tsconfig.node.json
├── .eslintrc.cjs
├── .prettierrc
├── public/
│   └── favicon.ico
├── src/
│   ├── main.tsx                    # 应用入口
│   ├── App.tsx                     # 根组件（Provider 组装）
│   ├── vite-env.d.ts
│   │
│   ├── routes/
│   │   ├── index.tsx               # 路由配置汇总
│   │   ├── ProtectedRoute.tsx      # 权限守卫路由包装
│   │   └── routes.ts               # 路由定义常量
│   │
│   ├── layouts/
│   │   ├── MainLayout.tsx          # 主布局（侧边栏 + Header + Content）
│   │   ├── MainLayout.less
│   │   ├── Sidebar.tsx             # 侧边导航栏
│   │   ├── Header.tsx              # 顶部栏（用户信息、语言切换、通知）
│   │   └── components/
│   │       ├── Logo.tsx            # 系统 Logo
│   │       ├── UserDropdown.tsx    # 用户下拉菜单
│   │       └── LanguageSwitcher.tsx# 语言切换
│   │
│   ├── features/                   # 按业务模块组织
│   │   ├── auth/
│   │   │   ├── pages/
│   │   │   │   ├── LoginPage.tsx
│   │   │   │   └── LoginPage.less
│   │   │   ├── stores/
│   │   │   │   └── authStore.ts     # Zustand store
│   │   │   ├── api/
│   │   │   │   └── authApi.ts       # 登录/登出/刷新 API
│   │   │   ├── hooks/
│   │   │   │   └── useAuth.ts
│   │   │   └── types.ts
│   │   │
│   │   ├── pipes/                   # 管材管理
│   │   │   ├── pages/
│   │   │   │   ├── SeamlessPipeListPage.tsx
│   │   │   │   ├── SeamlessPipeDetailPage.tsx
│   │   │   │   ├── SeamlessPipeFormPage.tsx
│   │   │   │   ├── ScreenPipeListPage.tsx
│   │   │   │   ├── ScreenPipeDetailPage.tsx
│   │   │   │   ├── ScreenPipeFormPage.tsx
│   │   │   │   └── UnifiedPipeSearchPage.tsx
│   │   │   ├── components/
│   │   │   │   ├── PipeFilterBar.tsx
│   │   │   │   ├── PipeTable.tsx
│   │   │   │   ├── PipeForm.tsx
│   │   │   │   ├── PipeDetailCard.tsx
│   │   │   │   ├── GradeTag.tsx
│   │   │   │   └── PipeStatusBadge.tsx
│   │   │   ├── api/
│   │   │   │   └── pipeApi.ts
│   │   │   ├── hooks/
│   │   │   │   ├── useSeamlessPipes.ts
│   │   │   │   └── useScreenPipes.ts
│   │   │   └── types.ts
│   │   │
│   │   ├── inventory/               # 库存管理
│   │   │   ├── pages/
│   │   │   │   ├── InventoryPage.tsx
│   │   │   │   ├── InboundListPage.tsx
│   │   │   │   ├── InboundFormPage.tsx
│   │   │   │   ├── OutboundListPage.tsx
│   │   │   │   ├── OutboundFormPage.tsx
│   │   │   │   ├── InventoryLogPage.tsx
│   │   │   │   ├── InventoryCheckPage.tsx
│   │   │   │   └── LocationManagePage.tsx
│   │   │   ├── components/
│   │   │   │   ├── InboundForm.tsx
│   │   │   │   ├── OutboundForm.tsx
│   │   │   │   ├── StockSummaryCards.tsx
│   │   │   │   ├── InventoryTable.tsx
│   │   │   │   └── LocationTree.tsx
│   │   │   ├── api/
│   │   │   │   └── inventoryApi.ts
│   │   │   ├── hooks/
│   │   │   │   └── useInventory.ts
│   │   │   └── types.ts
│   │   │
│   │   ├── quality/                 # 质量管理
│   │   │   ├── pages/
│   │   │   │   ├── QualityCertListPage.tsx
│   │   │   │   ├── QualityCertFormPage.tsx
│   │   │   │   ├── QualityTracePage.tsx
│   │   │   │   └── Api5ctRefPage.tsx
│   │   │   ├── components/
│   │   │   │   ├── CertFileUploader.tsx
│   │   │   │   ├── TraceTimeline.tsx
│   │   │   │   └── GradeCompareTable.tsx
│   │   │   ├── api/
│   │   │   │   └── qualityApi.ts
│   │   │   └── types.ts
│   │   │
│   │   ├── purchases/               # 采购管理（独立模块）
│   │   │   ├── pages/
│   │   │   │   ├── PurchaseOrderListPage.tsx
│   │   │   │   ├── PurchaseOrderDetailPage.tsx
│   │   │   │   ├── PurchaseOrderFormPage.tsx
│   │   │   │   ├── SupplierListPage.tsx
│   │   │   │   └── SupplierFormPage.tsx
│   │   │   ├── components/
│   │   │   │   ├── OrderStatusTag.tsx
│   │   │   │   └── SupplierSelect.tsx
│   │   │   ├── api/
│   │   │   │   ├── purchaseApi.ts
│   │   │   │   └── supplierApi.ts
│   │   │   └── types.ts
│   │   │
│   │   ├── sales/                   # 销售管理（独立模块）
│   │   │   ├── pages/
│   │   │   │   ├── SalesOrderListPage.tsx
│   │   │   │   ├── SalesOrderDetailPage.tsx
│   │   │   │   ├── SalesOrderFormPage.tsx
│   │   │   │   ├── CustomerListPage.tsx
│   │   │   │   └── CustomerFormPage.tsx
│   │   │   ├── components/
│   │   │   │   ├── OrderStatusTag.tsx
│   │   │   │   ├── CustomerSelect.tsx
│   │   │   │   └── AtpBadge.tsx
│   │   │   ├── api/
│   │   │   │   ├── salesApi.ts
│   │   │   │   └── customerApi.ts
│   │   │   └── types.ts
│   │   │
│   │   ├── data-io/                # 数据导入导出
│   │   │   ├── pages/
│   │   │   │   ├── ImportPage.tsx
│   │   │   │   └── ExportPage.tsx
│   │   │   ├── components/
│   │   │   │   ├── FileUploader.tsx
│   │   │   │   └── ImportResultTable.tsx
│   │   │   └── types.ts
│   │   │
│   │   ├── reports/                # 报表统计
│   │   │   ├── pages/
│   │   │   │   ├── InventoryReportPage.tsx
│   │   │   │   ├── TrendReportPage.tsx
│   │   │   │   └── DashboardPage.tsx
│   │   │   ├── components/
│   │   │   │   ├── KpiCards.tsx
│   │   │   │   ├── StockPieChart.tsx
│   │   │   │   ├── TrendLineChart.tsx
│   │   │   │   └── ReportFilterBar.tsx
│   │   │   └── api/
│   │   │       └── reportApi.ts
│   │   │
│   │   └── system/                 # 系统管理
│   │       ├── pages/
│   │       │   ├── UserListPage.tsx
│   │       │   ├── UserFormPage.tsx
│   │       │   ├── OperationLogPage.tsx
│   │       │   └── ProfilePage.tsx
│   │       ├── components/
│   │       │   ├── RoleTag.tsx
│   │       │   └── UserForm.tsx
│   │       ├── api/
│   │       │   └── userApi.ts
│   │       └── types.ts
│   │
│   ├── shared/                     # 共享层
│   │   ├── components/
│   │   │   ├── PageHeader.tsx       # 页面标题组件
│   │   │   ├── SearchBar.tsx        # 通用搜索栏
│   │   │   ├── ConfirmModal.tsx     # 二次确认弹窗
│   │   │   ├── LoadingSpin.tsx      # 加载中
│   │   │   ├── EmptyState.tsx       # 空状态
│   │   │   ├── ErrorBoundary.tsx    # 错误边界
│   │   │   └── PageContainer.tsx    # 页面容器（面包屑+内容）
│   │   ├── hooks/
│   │   │   ├── usePagination.ts     # 分页逻辑复用
│   │   │   ├── useUnitConvert.ts    # 单位转换 hook
│   │   │   └── usePermission.ts     # 权限判断 hook
│   │   ├── utils/
│   │   │   ├── format.ts            # 数据格式化
│   │   │   ├── unit-convert.ts      # 英制/公制转换工具函数
│   │   │   ├── pipe-number.ts       # 管材编号生成/解析
│   │   │   └── constants.ts         # 常量（钢级列表、端部类型等）
│   │   └── types/
│   │       ├── api.ts               # 统一 API 响应类型
│   │       └── common.ts            # 通用类型定义
│   │
│   ├── api/                        # API 基础设施
│   │   ├── client.ts               # Axios 实例 + 拦截器
│   │   ├── types.ts                # 通用请求/响应类型
│   │   └── queryClient.ts          # TanStack Query Client 配置
│   │
│   ├── stores/                     # 全局 Zustand stores
│   │   ├── authStore.ts            # 认证状态
│   │   ├── appStore.ts             # 应用状态（侧边栏折叠、主题等）
│   │   └── unitStore.ts            # 单位制偏好
│   │
│   ├── mocks/                      # MSW Mock API（仅开发环境）
│   │   ├── browser.ts              # Worker 初始化
│   │   ├── handlers.ts             # 路由 handler 定义
│   │   └── data/                   # Mock 数据生成
│   │       ├── pipes.ts
│   │       └── auth.ts
│   │
│   ├── i18n/                       # 国际化
│   │   ├── index.ts                # i18next 初始化
│   │   ├── resources/
│   │   │   ├── zh/
│   │   │   │   ├── common.json
│   │   │   │   ├── pipes.json
│   │   │   │   ├── inventory.json
│   │   │   │   ├── quality.json
│   │   │   │   ├── purchase.json
│   │   │   │   ├── sales.json
│   │   │   │   ├── system.json
│   │   │   │   └── validation.json
│   │   │   └── en/
│   │   │       ├── common.json
│   │   │       ├── pipes.json
│   │   │       ├── inventory.json
│   │   │       ├── quality.json
│   │   │       ├── purchase.json
│   │   │       ├── sales.json
│   │   │       ├── system.json
│   │   │       └── validation.json
│   │   └── locale.ts               # dayjs locale 配置
│   │
│   └── styles/                     # 全局样式
│       ├── global.less              # 全局样式覆盖
│       ├── theme.ts                # Ant Design 5 主题 Token
│       └── variables.less          # Less 变量
│
└── .env                            # 环境变量（API_BASE_URL）
```

---

## 3. 页面路由设计

### 3.1 路由结构

```
/login                                  # 登录页（公开）
/                                       # 主布局（需认证）
├── dashboard                           # 仪表盘首页
│
├── pipes                               # 管材管理
│   ├── seamless                        # 无缝钢管列表
│   ├── seamless/new                    # 新增无缝钢管
│   ├── seamless/:id                    # 无缝钢管详情
│   ├── seamless/:id/edit              # 编辑无缝钢管
│   ├── screen                          # 筛管列表
│   ├── screen/new                      # 新增筛管
│   ├── screen/:id                      # 筛管详情
│   ├── screen/:id/edit                # 编辑筛管
│   └── search                          # 统一管材搜索
│
├── inventory                           # 库存管理
│   ├── stock                           # 实时库存
│   ├── inbound                         # 入库记录
│   ├── inbound/new                     # 新增入库
│   ├── outbound                        # 出库记录
│   ├── outbound/new                    # 新增出库
│   ├── logs                            # 库存流水
│   ├── checks                          # 盘点管理
│   │   └── :id                         # 盘点详情
│   └── locations                       # 库位管理
│
├── quality                             # 质量管理
│   ├── certs                           # 质检证书
│   ├── certs/new                       # 新增质检证书
│   ├── certs/:id                       # 质检证书详情
│   ├── trace                           # 质量追溯
│   └── api5ct-ref                      # API 5CT 标准参考
│
├── purchases                           # 采购管理（独立模块）
│   ├── orders                          # 采购订单
│   │   ├── :id                         # 采购订单详情
│   │   └── new                         # 新增采购订单
│   └── suppliers                       # 供应商管理
│
├── sales                               # 销售管理（独立模块）
│   ├── orders                          # 销售订单
│   │   ├── :id                         # 销售订单详情
│   │   └── new                         # 新增销售订单
│   ├── customers                       # 客户管理
│   └── atp                             # 可售量查询
│
├── data-io                             # 数据导入导出
│   ├── import                          # 导入
│   └── export                          # 导出
│
├── reports                             # 报表统计
│   ├── inventory                       # 库存报表
│   ├── trends                          # 趋势分析
│   └── dashboard                       # 仪表盘（Dashboard 的别名）
│
└── system                              # 系统管理
    ├── users                           # 用户管理
    ├── users/:id                       # 用户编辑
    ├── logs                            # 操作日志
    └── profile                         # 个人设置
```

### 3.2 路由配置定义

```typescript
// src/routes/routes.ts
import { lazy } from 'react';

// 懒加载所有页面
const LoginPage = lazy(() => import('@/features/auth/pages/LoginPage'));
const DashboardPage = lazy(() => import('@/features/reports/pages/DashboardPage'));

// 管材管理
const SeamlessPipeList = lazy(() => import('@/features/pipes/pages/SeamlessPipeListPage'));
const SeamlessPipeForm = lazy(() => import('@/features/pipes/pages/SeamlessPipeFormPage'));
const ScreenPipeList = lazy(() => import('@/features/pipes/pages/ScreenPipeListPage'));
const ScreenPipeForm = lazy(() => import('@/features/pipes/pages/ScreenPipeFormPage'));

// ... 其他页面

export interface RouteConfig {
  path: string;
  element: React.LazyExoticComponent<React.ComponentType>;
  permissions?: Role[];    // 允许访问的角色
  titleKey: string;       // i18n key 用于页面标题
  hideInMenu?: boolean;   // 是否在菜单中隐藏
  icon?: React.ComponentType;
}

export const menuRoutes: RouteConfig[] = [
  { path: 'dashboard', element: DashboardPage, titleKey: 'menu.dashboard', icon: DashboardOutlined, permissions: ['admin', 'warehouse', 'qc', 'sales'] },

  // 管材管理
  { path: 'pipes/seamless', element: SeamlessPipeList, titleKey: 'menu.pipes.seamless', icon: NodeIndexOutlined, permissions: ['admin', 'warehouse', 'qc'] },
  { path: 'pipes/seamless/new', element: SeamlessPipeForm, titleKey: 'menu.pipes.seamless_new', hideInMenu: true, permissions: ['admin', 'warehouse'] },
  { path: 'pipes/screen', element: ScreenPipeList, titleKey: 'menu.pipes.screen', permissions: ['admin', 'warehouse', 'qc'] },

  // 库存管理
  { path: 'inventory/stock', element: InventoryPage, titleKey: 'menu.inventory.stock', icon: DatabaseOutlined, permissions: ['admin', 'warehouse', 'sales'] },
{ path: 'inventory/inbound', element: InboundListPage, titleKey: 'menu.inventory.inbound', icon: ImportOutlined, permissions: ['admin', 'warehouse', 'sales'] },
{ path: 'inventory/inbound/:id/approve', element: InboundApprovalPanel, titleKey: 'menu.inventory.inbound_approve', hideInMenu: true, permissions: ['admin', 'warehouse'] },
{ path: 'inventory/outbound', element: OutboundListPage, titleKey: 'menu.inventory.outbound', icon: ExportOutlined, permissions: ['admin', 'warehouse', 'sales'] },
{ path: 'inventory/outbound/:id/approve', element: OutboundApprovalPanel, titleKey: 'menu.inventory.outbound_approve', hideInMenu: true, permissions: ['admin', 'warehouse'] },

  // ... 其他模块
];
```

### 3.3 权限守卫

```typescript
// src/routes/ProtectedRoute.tsx
// 从 authStore 获取当前用户角色
// 与路由配置的 permissions 比对
// 无权限时显示 403 页面
// 未登录时重定向到 /login
```

---

## 4. 页面列表与功能

### 4.1 页面总览（共约 35 个页面）

| 模块 | 页面 | 说明 | 角色限制 |
|------|------|------|---------|
| **认证** | 登录页 | 用户名密码登录，中英文切换 | 公开 |
| | 个人设置 | 修改密码、语言偏好、单位制 | 全部 |
| **仪表盘** | 首页 | KPI 卡片（库存总量/出入库统计）、图表、快捷入口 | 全部 |
| **管材管理** | 无缝钢管列表 | 表格展示 + 高级筛选 + 批量操作 | admin/wh/qc |
| | 无缝钢管新增| 表单页，级联选择钢级/规格 | admin/wh |
| | 无缝钢管详情 | 展示所有字段 + 关联出入库记录 | admin/wh/qc |
| | 筛管列表 | 同无缝钢管 | admin/wh/qc |
| | 筛管新增/编辑 | 含筛管特有字段 | admin/wh |
| | 统一搜索 | 跨类型搜索 | 全部 |
| **库存管理** | 实时库存 | 聚合查询，按钢级/规格分组 | 全部 |
| | 入库记录列表 | 展示入库历史 | admin/wh |
| | 新增入库 | 选择管材 + 填写入库信息 | admin/wh |
| | 出库记录列表 | 展示出库历史 | admin/wh |
| | 新增出库 | 选择管材 + 填写出库信息 | admin/wh |
| | 库存流水 | 管材全生命周期变动记录 | admin/wh |
| | 盘点管理 | 创建/执行盘点，查看差异报告 | admin/wh |
| | 库位管理 | 库区/货架树形管理 | admin/wh |
| **质量管理** | 质检证书列表 | 展示质检记录 | admin/qc |
| | 新增质检证书 | 上传文件，填写检测结果 | admin/qc |
| | 质量追溯 | 按炉批号/管材编号查询追溯链 | admin/qc |
| | API 5CT 标准参考 | 查询各钢级力学性能参考 | 全部 |
| **采购管理** | 采购订单列表 | 采购订单 CRUD + 审核流程 | admin/sales |
| | 采购订单详情 | 订单明细 + 关联入库记录 | admin/sales |
| | 供应商管理 | 供应商信息维护 | admin/sales |
| **销售管理** | 销售订单列表 | 销售订单 CRUD + 审核流程 | admin/sales |
| | 销售订单详情 | 订单明细 + ATP + 出库记录 | admin/sales |
| | 客户管理 | 客户信息维护 | admin/sales |
| | 可售量查询 | 按规格查看 ATP | admin/sales/wh |
| **数据导入导出** | 导入页 | 上传文件，配置映射，查看导入结果 | admin/wh/qc |
| | 导出页 | 选择数据范围，生成 Excel | admin/wh/qc/sales |
| **报表统计** | 库存报表 | 按类型/钢级汇总 + 月度变动 | admin/wh |
| | 趋势分析 | 出入库趋势图 | admin |
| **系统管理** | 用户管理 | 用户列表 + 增删改 + 角色分配 | admin |
| | 操作日志 | 审计日志查询 | admin |
| | 个人设置 | 偏好设置 | 全部 |

### 4.2 核心页面原型描述

#### 登录页

```
┌─────────────────────────────────────────────┐
│                                             │
│            [系统 Logo]                       │
│       无缝钢管与筛管管理系统                   │
│                                             │
│  ┌─────────────────────────────────────┐    │
│  │  用户名                               │    │
│  │  [________________________]          │    │
│  │  密码                                │    │
│  │  [________________________]          │    │
│  │                                     │    │
│  │  [     登  录      ] [ En / 中文 ]  │    │
│  └─────────────────────────────────────┘    │
│                                             │
│  © 2026 Pipe Management System              │
└─────────────────────────────────────────────┘
```

#### 主布局

```
┌──────────┬──────────────────────────────────────────┐
│          │  ← 侧边栏折叠按钮  [ 搜索... ]  用户头像 ▼ │
│  侧边栏  ├──────────────────────────────────────────┤
│  导航    │  面包屑 > 当前页面                         │
│          │                                           │
│  一级菜单│  ┌─────────────────────────────────────┐  │
│  ───────│  │                                     │  │
│  📊 仪表盘│  │        页面内容区                    │  │
│  ───────│  │                                     │  │
│  🔩 管材  │  │  (Ant Design Pro 布局)              │  │
│  │ 无缝钢│  │                                     │  │
│  │ 筛管  │  │                                     │  │
│  │ 搜索  │  └─────────────────────────────────────┘  │
│  ───────│                                           │
│  📦 库存 │                                           │
│  ├ 实时库│                                           │
│  ├ 入库  │                                           │
│  ├ 出库  │                                           │
│  ├ 流水  │                                           │
│  ├ 盘点  │                                           │
│  └ 库位  │                                           │
│  ───────│                                           │
│  ✅ 质量 │                                           │
│  📋 采购 │                                           │
│  📊 报表 │                                           │
│  ⚙ 系统  │                                           │
└──────────┴──────────────────────────────────────────┘
```

#### 列表页通用模式（以无缝钢管列表为例）

```
┌─────────────────────────────────────────────────────┐
│  无缝钢管管理    [ + 新增 ] [ 导入 ] [ 导出 ]        │
│                                                     │
│  ┌─ 筛选条件 ─────────────────────────────────────┐ │
│  │  钢级: [下拉]  外径: [__]~[__]  壁厚: [__]~[__]│ │
│  │  状态: [下拉]  库位: [下拉]  搜索: [______]     │ │
│  │  [查询] [重置]                                 │ │
│  └───────────────────────────────────────────────┘ │
│                                                     │
│  ┌─ 表格 ──────────────────────────────────────────┐│
│  │ ☐ │ 管材编号 │ 钢级 │ 规格(OD×WT) │ 状态 │ 库位││
│  │ ☐ │ CSG-... │ J55  │ 4.5×0.250  │ ✔ 在库 │A-..││
│  │ ☐ │ CSG-... │ N80  │ 5.5×0.304  │ ➡ 已出 │B-..││
│  │ ☐ │ ...     │ ...  │ ...        │ ...   │... ││
│  │                                               ││
│  │  共 128 条   ◀ 1 2 3 4 5 ... 10 ▶  每页 20 条 ││
│  └───────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

#### 表单页通用模式

```
┌─────────────────────────────────────────────────────┐
│  新增无缝钢管    [返回]                              │
│                                                     │
│  ┌─ 基本信息 ─────────────────────────────────────┐ │
│  │  管材编号 *: [_____________________]  [自动生成]│ │
│  │  产品类型 *: ○ 套管  ○ 油管                    │ │
│  │  钢级 *:     [J55 ▼]                           │ │
│  │  外径 (in) *: [____]  壁厚 (in) *: [____]      │ │
│  │  长度 (ft):  [____]  单位重量(lb/ft): [____]   │ │
│  │  端部类型:    [SC ▼]                            │ │
│  └───────────────────────────────────────────────┘ │
│  ┌─ 接箍信息 ─────────────────────────────────────┐ │
│  │  接箍类型: [__________]  外径: [____]  长度:   │ │
│  └───────────────────────────────────────────────┘ │
│  ┌─ 生产信息 ─────────────────────────────────────┐ │
│  │  炉批号: [______________]  序列号: [_________] │ │
│  │  生产厂家: [______________]  生产日期: [📅__]  │ │
│  │  质检证书编号: [______________]                │ │
│  └───────────────────────────────────────────────┘ │
│                                                     │
│  [  提 交  ]  [  取 消  ]                           │
└─────────────────────────────────────────────────────┘
```

---

## 5. 布局结构

### 5.1 Ant Design Pro 布局

采用 Ant Design 5 的 `ProLayout` 组件（或 `Layout` 组件组合）：

```
┌──────────────────────────────────────────────────────────┐
│  Layout (flex container)                                  │
│                                                           │
│  ┌── Sider ──────────────────┬── Content ───────────────┐│
│  │                           │                          ││
│  │  Logo + 系统名称           │  Header                  ││
│  │                           │  ┌─────────────────────┐ ││
│  │  Menu (Ant Design Menu)   │  │ 折叠按钮 面包屑 语言│ ││
│  │  ├── 仪表盘               │  │ 切换 用户下拉       │ ││
│  │  ├── 管材管理 ▼           │  └─────────────────────┘ ││
│  │  │   ├── 无缝钢管         │                          ││
│  │  │   ├── 筛管             │  Page Content            ││
│  │  │   └── 统一搜索         │  ┌─────────────────────┐ ││
│  │  ├── 库存管理 ▼           │  │                     │ ││
│  │  │   ├── 实时库存         │  │  (页面内容)         │ ││
│  │  │   ├── 入库管理         │  │                     │ ││
│  │  │   ├── 出库管理         │  │                     │ ││
│  │  │   ├── 库存流水         │  └─────────────────────┘ ││
│  │  │   ├── 盘点管理         │                          ││
│  │  │   └── 库位管理         │                          ││
│  │  ├── 质量管理 ▼           │                          ││
│  │  ├── 采购管理 ▼           │                          ││
│  │  ├── 销售管理 ▼           │                          ││
│  │  ├── 数据导入导出 ▼       │                          ││
│  │  ├── 报表统计 ▼           │                          ││
│  │  └── 系统管理 ▼           │                          ││
│  │                           │                          ││
│  │  (折叠后只显示图标)        │                          ││
│  └───────────────────────────┴──────────────────────────┘│
└──────────────────────────────────────────────────────────┘
```

### 5.2 响应式断点

| 断点 | 宽度 | 侧边栏行为 |
|------|------|-----------|
| `xxl` | ≥1600px | 展开，220px 宽 |
| `xl` | 1200~1599px | 展开，220px 宽 |
| `lg` | 992~1199px | 折叠，只显示图标（80px） |
| `md` | 768~991px | 折叠，遮罩层弹出 |
| `sm` | <768px | 折叠，遮罩层弹出 |

### 5.3 Header 内容

| 区域 | 组件 | 说明 |
|------|------|------|
| 左侧 | 折叠按钮 + 面包屑导航 | 点击折叠/展开侧边栏 |
| 中间 | 全局搜索 | 快速搜索管材编号（快捷键 Ctrl+K） |
| 右侧 | 语言切换 | 中文 / English 切换按钮 |
| | 单位制切换 | Metric / Imperial 快速切换 |
| | 用户头像下拉 | 个人设置、退出登录 |

---

## 6. 组件树

### 6.1 组件层级

```
<App>
  <QueryClientProvider>       // TanStack Query
    <BrowserRouter>
      <AntdConfigProvider>    // Ant Design 5 主题
        <I18nextProvider>     // 国际化
        <Routes>
          ├── /login → <LoginPage>
          │
          └── / → <MainLayout>       // ProtectedRoute 包裹
                ├── <Sidebar>
                │   ├── <Logo />
                │   └── <Menu />      // 根据角色动态生成
                │
                ├── <Header>
                │   ├── <Breadcrumb />
                │   ├── <LanguageSwitcher />
                │   ├── <UnitSwitch />
                │   └── <UserDropdown />
                │
                └── <Content>
                    ├── <Outlet />    // 嵌套路由页面
                    │
                    ├── <PageHeader />        // 页面标题+操作按钮
                    ├── <PageContainer />     // 标准页面容器
                    │
                    ├── 列表页通用模板
                    │   ├── <FilterBar />
                    │   ├── <AntTable />      // 带分页
                    │   └── <ActionButtons />
                    │
                    └── 表单页通用模板
                        ├── <AntForm />
                        └── <FormActions />
```

### 6.2 共享组件清单

| 组件 | 位置 | 说明 |
|------|------|------|
| `PageHeader` | `shared/components` | 页面标题 + 面包屑 + 右侧操作按钮 |
| `PageContainer` | `shared/components` | 白色卡片容器，统一内边距 |
| `SearchBar` | `shared/components` | 通用模糊搜索输入框 |
| `ConfirmModal` | `shared/components` | 二次确认弹窗（删除/审核/取消操作） |
| `LoadingSpin` | `shared/components` | 全屏或区域级加载状态 |
| `EmptyState` | `shared/components` | 空数据占位 |
| `ErrorBoundary` | `shared/components` | React Error Boundary |
| `StatusTag` | `shared/components` | 通用状态标签（根据状态映射颜色） |
| `FileUploader` | `shared/components` | 文件上传组件（单文件/多文件） |

---

## 7. 状态管理方案

### 7.1 分层状态架构

```
┌─────────────────────────────────────────────────────────────┐
│                    客户端状态 (Zustand)                       │
│                                                             │
│  authStore          appStore          unitStore             │
│  ┌─────────────┐  ┌─────────────┐  ┌────────────┐          │
│  │ user        │  │ siderCollapsed│  │ unitSystem │         │
│  │ token       │  │ theme       │  │ (metric /  │         │
│  │ role        │  │ currentLang │  │  imperial) │         │
│  │ permissions │  │            │  └────────────┘          │
│  └─────────────┘  └─────────────┘                           │
├─────────────────────────────────────────────────────────────┤
│                   服务端状态 (TanStack Query)                 │
│                                                             │
│  useSeamlessPipes(queryKey: ['seamless_pipes', filters])    │
│  useScreenPipes(queryKey: ['screen_pipes', filters])        │
│  useInventory(queryKey: ['inventory', filters])             │
│  useInboundRecords(queryKey: ['inbound', filters])          │
│  useOutboundRecords(queryKey: ['outbound', filters])        │
│  usePurchaseOrders(queryKey: ['purchase_orders', filters])  │
│  useSalesOrders(queryKey: ['sales_orders', filters])        │
│  ...                                                         │
└─────────────────────────────────────────────────────────────┘
```

### 7.2 Zustand Store 定义

```typescript
// src/stores/authStore.ts — 纯状态，不做 API 调用
interface AuthState {
  user: User | null;
  token: string | null;
  refreshTokenValue: string | null;
  isAuthenticated: boolean;

  // 纯状态操作（不发起任何请求）
  setUser: (user: User) => void;
  setToken: (token: string, refreshToken: string) => void;
  logout: () => void;
}

// 说明：login() 由 React Hook（useLogin）调用 API 后通过 setToken 写入；
//       refreshToken 操作由 Axios 拦截器调用 API 后通过 setToken 写入。
```

### 7.3 TanStack Query 配置

```typescript
// src/api/queryClient.ts
const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 120_000,         // 2min 内认为数据新鲜（减少轮询请求）
      gcTime: 5 * 60_000,         // 5min 缓存
      retry: 2,                    // 失败重试 2 次
      refetchOnWindowFocus: false, // 不自动刷新（管理后台不适用）
    },
    mutations: {
      retry: 0,
    },
  },
});

// 缓存失效策略
// 管材变更 → invalidate ['seamless_pipes'], ['screen_pipes']
// 入库操作 → invalidate ['inbound'], ['inventory']
// 出库操作 → invalidate ['outbound'], ['inventory']
```

### 7.4 典型 Hook 示例

```typescript
// src/features/pipes/hooks/useSeamlessPipes.ts
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { pipeApi } from '@/features/pipes/api/pipeApi';
import type { SeamlessPipe, PipeFilter } from '@/features/pipes/types';

export function useSeamlessPipes(filters: PipeFilter, page: number, pageSize: number) {
  return useQuery({
    queryKey: ['seamless_pipes', filters, page, pageSize],
    queryFn: () => pipeApi.getSeamlessPipes(filters, page, pageSize),
    placeholderData: keepPreviousData,  // 翻页时保留旧数据
  });
}

export function useCreateSeamlessPipe() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (data: SeamlessPipeCreateDto) => pipeApi.createSeamlessPipe(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['seamless_pipes'] });
    },
  });
}
```

---

## 8. API 层设计

### 8.1 Axios 实例与拦截器

```typescript
// src/api/client.ts
import axios from 'axios';
import { authStore } from '@/stores/authStore';

const apiClient = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL || '/api/v1',
  timeout: 30_000,
  headers: { 'Content-Type': 'application/json' },
});

// 请求拦截器：自动注入 Token
apiClient.interceptors.request.use((config) => {
  const token = authStore.getState().token;
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

// 响应拦截器：统一错误处理 + Token 自动刷新（纯 Axios，不依赖 Store 方法）
let isRefreshing = false;
let failedQueue: Array<{ resolve: Function; reject: Function }> = [];

const processQueue = (error: unknown, token: string | null = null) => {
  failedQueue.forEach(({ resolve, reject }) => {
    if (token) resolve(token);
    else reject(error);
  });
  failedQueue = [];
};

// 刷新用的独立 Axios 实例（避免与拦截器循环调用）
const refreshClient = axios.create({ baseURL: import.meta.env.VITE_API_BASE_URL || '/api/v1' });

apiClient.interceptors.response.use(
  (response) => response.data,
  async (error) => {
    const originalRequest = error.config;

    if (error.response?.status !== 401 || originalRequest._retry) {
      return Promise.reject(error.response?.data?.error || error);
    }

    if (isRefreshing) {
      return new Promise((resolve, reject) => {
        failedQueue.push({ resolve, reject });
      }).then((token) => {
        originalRequest.headers.Authorization = `Bearer ${token}`;
        return apiClient(originalRequest);
      });
    }

    originalRequest._retry = true;
    isRefreshing = true;

    try {
      const store = authStore.getState();
      const { data } = await refreshClient.post('/auth/refresh', {
        refresh_token: store.refreshTokenValue,
      });
      const { access_token, refresh_token } = data;

      // 纯状态写入
      store.setToken(access_token, refresh_token);

      processQueue(null, access_token);
      originalRequest.headers.Authorization = `Bearer ${access_token}`;
      return apiClient(originalRequest);
    } catch (refreshError) {
      processQueue(refreshError, null);
      authStore.getState().logout();
      window.location.href = '/login';
      return Promise.reject(refreshError);
    } finally {
      isRefreshing = false;
    }
  }
);

export default apiClient;
```

### 8.2 类型安全的 API 函数

```typescript
// src/features/pipes/api/pipeApi.ts
import apiClient from '@/api/client';
import type {
  SeamlessPipe,
  ScreenPipe,
  PipeFilter,
  PaginatedResponse,
  SeamlessPipeCreateDto,
  ScreenPipeCreateDto,
} from '../types';

export const pipeApi = {
  // 无缝钢管
  getSeamlessPipes(filters: PipeFilter, page: number, pageSize: number) {
    return apiClient.get<PaginatedResponse<SeamlessPipe>>('/seamless-pipes', {
      params: { ...filters, page, page_size: pageSize },
    });
  },

  getSeamlessPipe(id: number) {
    return apiClient.get<SeamlessPipe>(`/seamless-pipes/${id}`);
  },

  createSeamlessPipe(data: SeamlessPipeCreateDto) {
    return apiClient.post<SeamlessPipe>('/seamless-pipes', data);
  },

  updateSeamlessPipe(id: number, data: SeamlessPipeCreateDto) {
    return apiClient.put<SeamlessPipe>(`/seamless-pipes/${id}`, data);
  },

  deleteSeamlessPipe(id: number) {
    return apiClient.delete(`/seamless-pipes/${id}`);
  },

  // 筛管 — 类似结构
  getScreenPipes(filters: PipeFilter, page: number, pageSize: number) {
    return apiClient.get<PaginatedResponse<ScreenPipe>>('/screen-pipes', {
      params: { ...filters, page, page_size: pageSize },
    });
  },

  // 统一搜索
  searchPipes(query: string) {
    return apiClient.get<{ seamless_pipes: SeamlessPipe[]; screen_pipes: ScreenPipe[] }>(
      '/pipes/search',
      { params: { q: query } }
    );
  },
};
```

### 8.3 统一类型定义

```typescript
// src/shared/types/api.ts
export interface ApiResponse<T> {
  success: boolean;
  data: T;
  meta?: PaginationMeta;
  request_id: string;
}

export interface ApiError {
  code: string;
  message: string;
  details?: Record<string, unknown>;
}

export interface PaginationMeta {
  page: number;
  page_size: number;
  total: number;
}

export interface PaginatedRequest {
  page?: number;
  page_size?: number;
  sort_by?: string;
  sort_order?: 'asc' | 'desc';
}
```

---

## 9. 认证与鉴权流程

### 9.1 登录流程

```
用户输入凭证
    │
    ▼
POST /api/v1/auth/login
    │
    ▼
接收 { access_token, refresh_token, expires_in, user }
    │
    ├── Login Hook 调用 authStore.setToken(access_token, refresh_token)
    ├── 存入 localStorage (持久化)
    └── axios 默认 header 设置 Authorization
    │
    ▼
跳转 /dashboard
    │
    ▼
后续请求自动携带 Bearer Token
```

### 9.2 Token 刷新机制

```
API 请求 → 401 响应
    │
    ▼
冻结后续请求（排队）
    │
    ▼
请求 POST /api/v1/auth/refresh { refresh_token }
    │
    ├── 成功 → authStore.setToken(new_access, new_refresh)
    │          → 重放排队请求 → 继续
    │
    └── 失败 → authStore.logout() → 跳转 /login
```

### 9.3 路由级权限控制

```typescript
// ProtectedRoute 核心逻辑
function ProtectedRoute({ route }: { route: RouteConfig }) {
  const { isAuthenticated, user } = useAuthStore();

  if (!isAuthenticated) {
    return <Navigate to="/login" replace />;
  }

  const requiredRoles = route.permissions;
  if (requiredRoles && !requiredRoles.includes(user!.role)) {
    return <ForbiddenPage />;  // 403 页面
  }

  return <Outlet />;
}
```

### 9.4 菜单级权限控制

```typescript
// Sidebar 中根据角色过滤菜单
const menuItems = menuRoutes
  .filter(route => !route.hideInMenu)
  .filter(route => route.permissions?.includes(user.role))
  .map(route => ({
    key: route.path,
    icon: route.icon ? <route.icon /> : null,
    label: t(route.titleKey),
    children: route.children?.filter(child => child.permissions?.includes(user.role)),
  }));
```

---

## 10. 国际化方案

### 10.1 i18next 初始化

```typescript
// src/i18n/index.ts
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import zhCommon from './resources/zh/common.json';
import enCommon from './resources/en/common.json';
// ... 其他命名空间

i18n.use(initReactI18next).init({
  resources: {
    zh: {
      common: zhCommon,
      pipes: zhPipes,
      inventory: zhInventory,
      quality: zhQuality,
      purchase: zhPurchase,
      sales: zhSales,
      system: zhSystem,
      validation: zhValidation,
    },
    en: {
      common: enCommon,
      pipes: enPipes,
      inventory: enInventory,
      // ...
    },
  },
  lng: localStorage.getItem('language') || 'zh',
  fallbackLng: 'zh',
  ns: ['common', 'pipes', 'inventory', 'quality', 'purchase', 'sales', 'system', 'validation'],
  defaultNS: 'common',
  interpolation: {
    escapeValue: false,  // React 已做 XSS 防护
  },
});
```

### 10.2 翻译文件示例

```json
// i18n/resources/zh/pipes.json
{
  "pipe_number": "管材编号",
  "grade": "钢级",
  "od": "外径",
  "wt": "壁厚",
  "length": "长度",
  "end_type": "端部类型",
  "heat_number": "炉批号",
  "status": {
    "in_stock": "在库",
    "outbound": "已出库",
    "scrapped": "报废"
  }
}

// i18n/resources/en/pipes.json
{
  "pipe_number": "Pipe Number",
  "grade": "Grade",
  "od": "O.D.",
  "wt": "W.T.",
  "length": "Length",
  "end_type": "End Type",
  "heat_number": "Heat Number",
  "status": {
    "in_stock": "In Stock",
    "outbound": "Outbound",
    "scrapped": "Scrapped"
  }
}
```

### 10.3 Hook 使用

```typescript
// 在组件中使用
import { useTranslation } from 'react-i18next';

function PipeTable() {
  const { t, i18n } = useTranslation('pipes');

  return (
    <Table columns={[
      { title: t('pipe_number'), dataIndex: 'pipe_number' },
      { title: t('grade'), dataIndex: 'grade' },
      { title: t('status.in_stock'), dataIndex: 'status' },
    ]} />
  );
}
```

---

## 11. Ant Design 5 主题定制

### 11.1 工业风主题配置

```typescript
// src/styles/theme.ts
import type { ThemeConfig } from 'antd';

const theme: ThemeConfig = {
  token: {
    // 品牌色 - 工业蓝，代表专业/可靠
    colorPrimary: '#1B3A5C',         // 深海蓝
    colorInfo: '#1B3A5C',
    colorSuccess: '#389E0D',         // 丛林绿 - 用于"在库"状态
    colorWarning: '#D48806',         // 金黄 - 用于警告
    colorError: '#CF1322',           // 红 - 用于错误/报废

    // 中性色
    colorBgLayout: '#F0F2F5',        // 页面背景
    colorBgContainer: '#FFFFFF',     // 卡片背景
    colorText: '#1A1A1A',            // 正文颜色
    colorTextSecondary: '#595959',   // 次要文字

    // 字体 - 使用系统字体，不引入额外字体包
    // 中文回退：Microsoft YaHei / PingFang SC；英文：SF Pro / Segoe UI
    fontFamily: `-apple-system, BlinkMacSystemFont, 'Segoe UI', 'PingFang SC', 'Microsoft YaHei', sans-serif`,
    fontFamilyCode: `'SF Mono', 'Cascadia Code', 'Consolas', 'Courier New', monospace`,

    // 圆角
    borderRadius: 6,
    borderRadiusLG: 8,

    // 间距
    margin: 16,
    padding: 16,
    paddingContentHorizontal: 24,
    paddingContentVertical: 16,

    // 表格
    tableHeaderBg: '#F5F7FA',
    tableRowHoverBg: '#E6F0FF',
  },
  components: {
    Menu: {
      // 侧边栏菜单
      itemBg: 'transparent',
      subMenuItemBg: 'transparent',
      itemColor: '#FFFFFF',
      itemHoverColor: '#FFFFFF',
      itemHoverBg: 'rgba(255,255,255,0.12)',
      itemSelectedColor: '#FFFFFF',
      itemSelectedBg: 'rgba(255,255,255,0.2)',
      iconSize: 18,
    },
    Table: {
      // 数据表格 - 紧凑、信息密集
      headerBg: '#F5F7FA',
      headerColor: '#1A1A1A',
      headerBorderRadius: 6,
      rowHoverBg: '#E6F0FF',
      cellFontSize: 13,
    },
    Form: {
      labelColor: '#262626',
      verticalLabelMargin: 4,
    },
    Card: {
      paddingLG: 20,
    },
    Button: {
      primaryShadow: '0 2px 0 rgba(27,58,92,0.1)',
    },
  },
};

export default theme;
```

### 11.2 侧边栏暗色主题

侧边栏使用深色主题以突出导航，与内容区形成清晰视觉分区：

```
Sider Background:  #0F1A2E (深海军蓝)
Menu Text:         #FFFFFF / rgba(255,255,255,0.65)
Active Indicator:  #3B82F6 (亮蓝高亮)
Divider:           rgba(255,255,255,0.1)
```

### 11.3 卡片与页面容器规范

| 元素 | 样式 |
|------|------|
| **页面容器** | 白色背景，12px 内边距，6px 圆角 |
| **筛选栏** | 灰色背景 `#FAFAFA`，16px 内边距，底部 16px 间距 |
| **表格** | 紧凑模式 `size="small"`，斑马纹可选 |
| **表单卡片** | 分组用 Card 包裹，每组标题 14px 加粗 |
| **统计卡片** | Ant Design `Statistic` 组件，左侧图标 + 右侧数值 |

### 11.4 状态颜色映射

| 状态 | 颜色 | Ant Design Token |
|------|------|-----------------|
| 在库 (in_stock) | 绿色 | `colorSuccess` |
| 已出库 (outbound) | 橙色 | `colorWarning` |
| 报废 (scrapped) | 红色 | `colorError` |
| 草稿 (draft) | 灰色 | `default` |
| 待审核 (pending) | 金色 | `gold` |
| 已审核 (approved) | 蓝色 | `blue` |
| 已完成 (completed) | 绿色 | `colorSuccess` |
| 已取消 (cancelled) | 灰色 | `#D9D9D9` |

---

## 12. 前端数据流总图

```
用户操作 (点击/输入/提交)
    │
    ▼
React 组件
    │
    ├── 客户端状态变更 → Zustand Store → 订阅组件重新渲染
    │
    └── 服务端数据请求 → TanStack Query Hook
            │
            ▼
        API 函数 (pipeApi.getSeamlessPipes)
            │
            ▼
        Axios 拦截器 (注入 Token)
            │
            ▼
        HTTP Request (JSON)
            │
            ▼
        后端 API (/api/v1/seamless-pipes)
            │
            ▼
        HTTP Response (JSON)
            │
            ▼
        Axios 拦截器 (统一错误处理)
            │
            ▼
        TanStack Query (缓存 + 状态管理)
            │
            ├── loading → 组件显示 Skeleton/Spin
            ├── success → 组件显示数据
            └── error → 组件显示 Error 提示

数据变更操作 (创建/更新/删除)
    │
    ▼
TanStack Query Mutation
    │
    ▼
API 请求 → 成功
    │
    ▼
queryClient.invalidateQueries  →  列表页自动刷新
```

---

## 13. 后续实施建议

### 13.1 实施顺序建议

| 阶段 | 内容 | 工作量估算 |
|------|------|-----------|
| **Phase 1** | 项目脚手架搭建 + 布局 + 路由 + 认证 | 2-3 天 |
| **Phase 2** | 管材管理（列表/表单/详情）+ 搜索 | 3-4 天 |
| **Phase 3** | 库存管理（入库/出库/库存查询/库位） | 3-4 天 |
| **Phase 4** | 质量管理（质检证书/追溯/API 5CT 参考） | 2-3 天 |
| **Phase 5** | 采购与销售管理（订单/供应商/客户） | 3-4 天 |
| **Phase 6** | 数据导入导出 + 系统管理（用户/日志） | 2-3 天 |
| **Phase 7** | 报表与统计 + Dashboard 仪表盘 | 2-3 天 |
| **Phase 8** | 国际化完善 + 单位切换 + 标签打印 | 2 天 |

### 13.2 开发要点

1. **使用 Vite Proxy 处理跨域**：开发时 `/api` 请求代理到后端；**不使用 React Router loader**（所有数据依赖 TanStack Query 在组件内 fetch）
2. **Mock API**：使用 MSW (Mock Service Worker) 进行前端独立开发（详见 13.3）
3. **TypeScript 严格模式**：所有 API 响应类型化，运行时用 Zod 校验关键数据
4. **Ant Design Table 性能**：大数据量使用 `virtualized` 或分页控制
5. **统一错误处理**：Axios 拦截器（含 401 自动刷新） + `ErrorBoundary`（捕获渲染异常，显示降级 UI 并允许重试） + Ant Design `message` / `notification` 通知
6. **标签打印**：后端生成 PDF（`printpdf` crate），前端通过 `POST /api/v1/pipes/{id}/print-label` 下载 PDF 文件后调起浏览器打印

### 13.3 Mock API 设计（MSW）

```typescript
// src/mocks/handlers.ts — 示例
import { http, HttpResponse } from 'msw';
import { faker } from '@faker-js/faker/locale/zh_CN';

// 模拟分页响应
function paginated<T>(items: T[], page: number, pageSize: number) {
  const start = (page - 1) * pageSize;
  return {
    success: true,
    data: items.slice(start, start + pageSize),
    meta: { page, page_size: pageSize, total: items.length },
    request_id: crypto.randomUUID(),
  };
}

export const handlers = [
  // 无缝钢管列表
  http.get('/api/v1/seamless-pipes', ({ request }) => {
    const url = new URL(request.url);
    const page = Number(url.searchParams.get('page')) || 1;
    const pageSize = Number(url.searchParams.get('page_size')) || 20;
    const pipes = Array.from({ length: 53 }, (_, i) => ({
      id: i + 1,
      pipe_number: `J55 4.500in×11.60lb SC-H2405-${String(i + 1).padStart(6, '0')}`,
      grade: faker.helpers.arrayElement(['J55', 'N80', 'L80', 'P110']),
      od: Number(faker.number.float({ min: 4, max: 9, fractionDigits: 3 })),
      wt: Number(faker.number.float({ min: 0.2, max: 0.6, fractionDigits: 3 })),
      length: Number(faker.number.float({ min: 8, max: 13, fractionDigits: 2 })),
      status: faker.helpers.arrayElement(['in_stock', 'out_stock', 'qc_hold']),
      heat_number: `HT${faker.date.recent({ days: 90 }).toISOString().slice(0, 10).replace(/-/g, '')}-${String(Math.floor(i / 20) + 1).padStart(2, '0')}`,
    }));
    return HttpResponse.json(paginated(pipes, page, pageSize));
  }),

  // 认证
  http.post('/api/v1/auth/login', async ({ request }) => {
    const body = await request.json() as any;
    if (body.username === 'admin' && body.password === 'admin123') {
      return HttpResponse.json({
        success: true,
        data: {
          access_token: `mock_access_${Date.now()}`,
          refresh_token: `mock_refresh_${Date.now()}`,
          expires_in: 3600,
          user: { id: 1, username: 'admin', role: 'admin', display_name: '管理员' },
        },
        request_id: crypto.randomUUID(),
      });
    }
    return HttpResponse.json(
      { success: false, error: { code: 'AUTH_INVALID_CREDENTIALS', message: '用户名或密码错误' } },
      { status: 401 }
    );
  }),

  // Token 刷新（mock 成功）
  http.post('/api/v1/auth/refresh', () => {
    return HttpResponse.json({
      success: true,
      data: {
        access_token: `mock_refreshed_access_${Date.now()}`,
        refresh_token: `mock_refreshed_refresh_${Date.now()}`,
        expires_in: 3600,
      },
      request_id: crypto.randomUUID(),
    });
  }),

  // 更多 handler...
];

// src/mocks/browser.ts
import { setupWorker } from 'msw/browser';
export const worker = setupWorker(...handlers);

// src/main.ts — 开发环境启用 MSW
if (import.meta.env.DEV) {
  const { worker } = await import('./mocks/browser');
  await worker.start({ onUnhandledRequest: 'bypass' });
}
```

> **Mock 开发原则**：
> - MSW 只在 `import.meta.env.DEV` 下启用，不影响生产包
> - 使用 `@faker-js/faker` 生成逼真的中文数据
> - Mock handler 与 API 类型严格对应（共享 `types.ts`）
> - 实现时逐步用真实 API 替换 mock handler，无需修改组件代码

---

## 附录：关键第三方库速查

| 用途 | 库 | 说明 |
|------|-----|------|
| 条形码 | `jsbarcode` | 生成 Code128 / EAN 等格式条形码 |
| 二维码 | `qrcode.react` | 生成 QR Code |
| Excel 导出 | `xlsx` (SheetJS) | 前端直接生成 Excel 文件 |
| 日期格式化 | `dayjs` (Ant Design 内置) | — |
| 拖拽排序 | `@dnd-kit/core` | 如需表格拖拽排序 |
| 树形选择 | Ant Design TreeSelect | 库位选择 |
| 虚拟滚动 | `rc-virtual-list` (Ant Design 内置) | Table 大数据量优化 |
