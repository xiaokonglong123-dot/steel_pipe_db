# ── Rust / backend ──
/backend/target/
/backend/data/
*.db
*.db-journal
*.db-wal
*.db-shm

# ── Node / frontend (bun + Vue 3 + Vite) ──
/frontend/node_modules/
/frontend/dist/
/frontend/.vite/
/frontend/tsconfig.tsbuildinfo
/frontend/tsconfig.node.tsbuildinfo
*.tsbuildinfo

# ── Environment ──
.env
.env.local
.env.*.local
backend/.env
frontend/.env

# ── IDE ──
.idea/
.vscode/
*.swp
*.swo
*~
*.iml

# ── OS ──
.DS_Store
Thumbs.db

# ── Logs ──
*.log

# ── Python (seed scripts, ad-hoc) ──
__pycache__/
*.py[cod]
*.egg-info/
venv/
ENV/
env/

# ── Docker ──
docker-data/

# ── Tool / agent caches (local only, never commit) ──
.omo/
.opencode/
.pi/
.crew/
.superpowers/
.codegraph/
.cortexkit/
.worktrees/
.agents/
backups/
skills/
memory.md

# ── Local-only decision docs (never commit) ──
.local-only-docs/
docs/rewrite-tech-stack-comparison.md
